[project]
name = "rlm-engine"
version = "0.1.0"
description = "Recursive Language Model engine — analyze files beyond context limits"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0.0",
    "fastmcp>=2.0.0,<3",
]
